# there are two types of modules 
# -Internal modules
# -External modules

import math
import recursion

print(math.sqrt(16))
print(recursion.fibo(6)) #this will print first the output of recursion file there the same value (6) is passed thus op is 8 4.0 8